# Post-Processing Utilities

This directory contains compact helper scripts for curve comparison and scalar error-metric evaluation.

The scripts assume CSV input with one independent-variable column and one or more response columns. They are intentionally minimal and document the metric definitions used for the CMAME article rather than providing a complete plotting workflow.
